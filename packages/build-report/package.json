{
  "name": "@ts-doctor/build-report-effect",
  "version": "0.1.0",
  "description": "Effect-native rewrite of ts-doctor's versioned JSON report builder (RULE-004 summary rollup, RULE-034 schema version & ok). Strangler-fig slice — consumes @ts-doctor/score-effect for the monorepo MIN score (RULE-003). Pure functions + effect/Schema wire contract.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ts-doctor/contracts-effect": "workspace:*",
    "@ts-doctor/score-effect": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
