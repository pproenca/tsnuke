{
  "name": "@ts-doctor/capabilities-effect",
  "version": "0.1.0",
  "description": "Effect-native rewrite of ts-doctor's capability-gated rule-activation predicate (RULE-019/020). Strangler-fig slice — pure synchronous predicate on the Effect ecosystem (Schema contract).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ts-doctor/contracts-effect": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
