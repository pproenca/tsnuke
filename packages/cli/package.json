{
  "name": "ts-fix",
  "version": "0.1.0",
  "description": "The user-facing `ts-fix` CLI, re-imagined on `@effect/cli`: POSIX flag parsing, auto-help, and RULE-028 flag-exclusivity encoded as `Options` constraints. Wires the finished engine + format + fix-applier + build-report + exit-code slices into the default `inspect` command and the `install` subcommand. Equivalence is BEHAVIORAL (same flags accepted, same validation rejections, same output content, same exit codes), not a byte-verbatim port of the hand-rolled argv parser.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ts-fix": "./dist/cli.js"
  },
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/cli": "^0.75.1",
    "@effect/platform": "^0.96.1",
    "@effect/platform-node": "^0.106.0",
    "@effect/printer": "^0.49.0",
    "@effect/printer-ansi": "^0.49.0",
    "@ts-fix/build-report-effect": "workspace:*",
    "@ts-fix/contracts-effect": "workspace:*",
    "@ts-fix/engine-effect": "workspace:*",
    "@ts-fix/exit-code-effect": "workspace:*",
    "@ts-fix/fix-applier-effect": "workspace:*",
    "@ts-fix/format-effect": "workspace:*",
    "@ts-fix/rules-registry-effect": "workspace:*",
    "effect": "^3.21.2",
    "typescript": "^5.8.0"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@types/node": "^22.10.0",
    "esbuild": "0.27.7",
    "vitest": "^3.2.0"
  }
}
