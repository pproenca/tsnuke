{
  "name": "@ts-fix/discovery-effect",
  "private": true,
  "version": "0.1.0",
  "description": "Effect-native rewrite of ts-fix's project discovery + capability earning (RULE-012 file caps, RULE-021 capability earning, RULE-022 discovery validity). The biggest core module — an EFFECTFUL strangler-fig slice: filesystem discovery over @effect/platform FileSystem + Path with Node/in-memory Layers, typed errors on the Effect error channel (from @ts-fix/errors-effect), plus the PURE computeCapabilities derivation.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@effect/platform": "^0.96.1",
    "@effect/platform-node": "^0.106.0",
    "@ts-fix/contracts-effect": "workspace:*",
    "@ts-fix/errors-effect": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@effect/vitest": "^0.29.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
