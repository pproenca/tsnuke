{
  "name": "@ts-doctor/errors-effect",
  "version": "0.1.0",
  "description": "Effect-native rewrite of ts-doctor's tagged discovery error classes (RULE-037). Strangler-fig slice — idiomatic effect/Data tagged errors preserving the legacy _tag/name/instanceof-Error/cause contract that build-report's serializeError depends on.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
