{
  "name": "@ts-fix/exit-code-effect",
  "version": "0.1.0",
  "description": "Effect-native rewrite of ts-fix's exit-code gate (RULE-030/031). Strangler-fig slice — pure synchronous functions on the Effect ecosystem (Schema literals/branded types).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
