{
  "name": "@ts-doctor/format-effect",
  "version": "0.1.0",
  "description": "Effect-TS slice for ts-doctor's PURE output formatters: formatAgentReport (RULE-032 cheapest-action-first agent report), renderScoreLine/renderPretty (terminal output), and the offline explain/explainDiagnostic (--explain text). Plain pure functions over structural inputs — consumes @ts-doctor/contracts-effect for the Diagnostic/RuleMeta contract. NOT Effect-wrapped (no IO).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ts-doctor/contracts-effect": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
