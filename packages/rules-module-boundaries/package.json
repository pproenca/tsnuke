{
  "name": "@ts-doctor/rules-module-boundaries-effect",
  "version": "0.1.0",
  "description": "Effect-native rewrite of ts-doctor's `module-boundaries` SYN rule category (RULE-011 deep relative import >=4 leading `..`, no-default-export, public-api-must-be-explicit). Three pure AST predicates on the @ts-doctor/rules-core-effect substrate (defineRule/runRule), consuming the canonical Diagnostic/RuleMeta from @ts-doctor/contracts-effect. Plain-TS visitors over the TS compiler API — NOT Effect-wrapped (sync AST walk). The GRAPH rule `no-import-cycles` is DEFERRED to the module-graph batch.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ts-doctor/contracts-effect": "workspace:*",
    "@ts-doctor/rules-core-effect": "workspace:*",
    "typescript": "^5.8.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "vitest": "^3.2.0"
  }
}
