{
  "name": "@tsnuke/rules-registry-effect",
  "private": true,
  "version": "0.1.0",
  "description": "The GLOBAL rule registry: aggregates all 88 transformed rules (86 per-file SYN/TYP/CFG + 2 GRAPH) into the two registries the engine consumes (`ruleRegistry` + `graphRuleRegistry`). Effect-native replacement for the legacy codegen `rule-registry.generated.ts` — a hand-assembled aggregator over the per-category rule slices (import read-only; owns no rules itself).",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/main/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tsnuke/contracts-effect": "workspace:*",
    "@tsnuke/rules-core-effect": "workspace:*",
    "@tsnuke/rules-type-performance-effect": "workspace:*",
    "@tsnuke/rules-declaration-api-effect": "workspace:*",
    "@tsnuke/rules-security-effect": "workspace:*",
    "@tsnuke/rules-naming-idioms-effect": "workspace:*",
    "@tsnuke/rules-generics-effect": "workspace:*",
    "@tsnuke/rules-type-assertions-effect": "workspace:*",
    "@tsnuke/rules-async-effect": "workspace:*",
    "@tsnuke/rules-error-handling-effect": "workspace:*",
    "@tsnuke/rules-type-safety-effect": "workspace:*",
    "@tsnuke/rules-exhaustiveness-effect": "workspace:*",
    "@tsnuke/rules-module-boundaries-effect": "workspace:*",
    "@tsnuke/rules-graph-effect": "workspace:*",
    "effect": "^3.21.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
