{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "typecheck": { "dependsOn": ["^build"] },
    "gen": { "cache": false },
    "gen:check": { "dependsOn": ["gen"] },
    "test": { "dependsOn": ["^build"] }
  }
}
